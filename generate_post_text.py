"""
妄想会話風 投稿テキスト生成モジュール

Gemini APIを使用して、妄想会話風のエッチな投稿文を動的に生成する。
text_index をシードに使い、シチュエーション×トーンの組み合わせを
決定的に選ぶことで、履歴管理なしで重複を回避する。
（40シチュエーション × 8トーン = 320通り一巡まで同じペアなし）
"""

import os
from pathlib import Path

import requests


# シチュエーション（テーマ）プール
SITUATIONS = [
    "彼女が仕事帰りに甘えてくる",
    "同棲中の彼女がお風呂上がりにバスタオル姿で",
    "年上のお姉さんがバーで隣に座ってきた",
    "幼なじみの女の子が久しぶりに再会して大人になってた",
    "後輩の女の子が残業中に二人きりになって",
    "隣の部屋の女の子が薄着で訪ねてきた",
    "マッチングアプリで会った子が予想以上に積極的",
    "元カノから深夜に「会いたい」とLINE",
    "女友達と飲みすぎて終電逃して",
    "彼女が寝起きでまだ半分寝ぼけながら",
    "先輩の彼女が酔って甘えモード",
    "合コンで出会った子と二次会で二人きり",
    "彼女が新しい下着を見せてくる",
    "カフェで隣の席の美人がずっとこっち見てる",
    "彼女がヤキモチ焼いて拗ねてる",
    "温泉旅行で彼女と部屋に戻って",
    "雨の日に相合傘で密着",
    "彼女が料理中にエプロン姿で振り向く",
    "夏祭りで浴衣の彼女とはぐれて二人きり",
    "彼女が「今日は特別な日だから」と意味深に笑う",
    "深夜のドライブで彼女が助手席でうとうと",
    "初めてのお泊りデートで緊張してる彼女",
    "映画館で暗闘の中、彼女の手が触れてくる",
    "真夏の夜、彼女が「暑いから脱いでいい？」",
    "出張先のホテルで彼女からビデオ通話",
    "彼女がコスプレして驚かせてくる",
    "朝起きたら彼女がすでに起きてて見つめてた",
    "彼女が「内緒の話があるの」と耳元で囁く",
    "大学の図書館で気になるあの子と二人きり",
    "彼女が酔って「好き」を連呼してくる",
    "元カノと偶然再会して懐かしい空気に",
    "年下の女の子が「教えてください」と上目遣い",
    "彼女がマッサージしてあげると言い出す",
    "同僚の女の子が「相談がある」と個室に誘う",
    "ナースの彼女が仕事帰りにまだ制服で",
    "彼女が「写真撮って」とポーズを取る",
    "クリスマスイブに彼女が特別な格好で待ってた",
    "海で彼女が新しい水着を披露",
    "彼女の部屋に初めて行ったらいい匂いがして",
    "終電後のタクシーで彼女がもたれかかってくる",
]

# 会話トーン（口調のバリエーション）
TONES = [
    "甘えた口調の女の子",
    "ちょっとSっ気のあるお姉さん",
    "恥ずかしがり屋で小声の女の子",
    "積極的で大胆な女の子",
    "天然でいつの間にか距離が近い子",
    "クールだけど実は照れてる子",
    "ちょっと酔ってて素直になってる子",
    "いたずらっ子っぽくて挑発的な子",
]


def get_situation_and_tone(text_index: int) -> tuple[str, str]:
    """
    text_index から決定的にシチュエーションとトーンの組み合わせを選択。
    40 × 8 = 320 通りが一巡するまで同じペアは来ない。
    """
    total_combos = len(SITUATIONS) * len(TONES)
    combo_index = text_index % total_combos
    
    situation_idx = combo_index % len(SITUATIONS)
    tone_idx = combo_index // len(SITUATIONS)
    
    return SITUATIONS[situation_idx], TONES[tone_idx]


def generate_post_text_gemini(
    api_key: str,
    text_index: int = 0,
) -> str:
    """
    Gemini APIを使って妄想会話風の投稿文を生成する。
    text_index でシチュエーション×トーンが決まるため、
    履歴管理なしで自然に多様性が保たれる。
    
    Args:
        api_key: Gemini API Key
        text_index: 現在のテキストインデックス（post_status.jsonのtext_index）
    
    Returns:
        生成された投稿文
    """
    situation, tone = get_situation_and_tone(text_index)
    
    print(f"  シチュエーション: {situation}")
    print(f"  トーン: {tone}")

    prompt = f"""あなたはTwitter（X）の裏垢女子（AI美女）です。
「バズる」エッチな投稿文を1つ生成してください。

【シチュエーション】{situation}
【今の気分・口調】{tone}

【重要ルール】
1. **ほぼセリフのみ**で構成してください。（情景描写は（）で補足する程度、または無しでOK）
2. タイムラインでパッと目に止まる、**破壊力のある短い一言**を意識して。
3. 男が思わずリプライしたくなるような「隙」や「挑発」を含める。
4. 改行を効果的に使い、空白を作ることで「焦らし」を演出する。
5. 絵文字は文末に少しだけ（💕🥺🙈💦 など）。多すぎないこと。
6. ハッシュタグは不要。
7. 長々と書かない。一瞬でドキッとさせること。

【生成例イメージ】
「ねぇ、ここ…空いてるよ？💕」

「我慢、しなくていいのに…🥺」

「バレないように…静かにしてね？🤫💦」

【出力】投稿文のみを出力してください。"""

    try:
        url = f"https://generativelanguage.googleapis.com/v1beta/models/gemini-2.0-flash:generateContent?key={api_key}"
        
        payload = {
            "contents": [
                {
                    "parts": [
                        {"text": prompt}
                    ]
                }
            ],
            "generationConfig": {
                "temperature": 1.2,
                "topP": 0.95,
                "topK": 40,
                "maxOutputTokens": 500,
            },
            "safetySettings": [
                {"category": "HARM_CATEGORY_SEXUALLY_EXPLICIT", "threshold": "BLOCK_NONE"},
                {"category": "HARM_CATEGORY_HATE_SPEECH", "threshold": "BLOCK_NONE"},
                {"category": "HARM_CATEGORY_HARASSMENT", "threshold": "BLOCK_NONE"},
                {"category": "HARM_CATEGORY_DANGEROUS_CONTENT", "threshold": "BLOCK_NONE"},
            ]
        }
        
        response = requests.post(url, json=payload, timeout=30)
        response.raise_for_status()
        
        data = response.json()
        
        # レスポンスからテキストを抽出
        candidates = data.get("candidates", [])
        if not candidates:
            print("警告: Geminiからの応答が空です")
            return None
        
        text = candidates[0].get("content", {}).get("parts", [{}])[0].get("text", "")
        
        # 前後の空白・改行をトリム
        text = text.strip()
        
        # コードブロック記号の除去
        if text.startswith("```"):
            text = text.strip("`").strip()
        
        if text:
            print(f"  ✓ Gemini生成テキスト ({len(text)}文字)")
            return text
        else:
            print("警告: 生成テキストが空でした")
            return None
            
    except Exception as e:
        print(f"✗ Gemini API エラー: {e}")
        return None


# テスト用
if __name__ == "__main__":
    from dotenv import load_dotenv
    load_dotenv(Path(__file__).parent / ".env")
    
    api_key = os.getenv("GEMINI_API_KEY")
    if not api_key:
        print("GEMINI_API_KEY が設定されていません")
        exit(1)
    
    print("=== 妄想会話テキスト生成テスト ===\n")
    
    for i in range(3):
        print(f"\n--- テスト (index={i}) ---")
        text = generate_post_text_gemini(api_key, text_index=i)
        if text:
            print(f"\n{text}\n")
        print("-" * 40)
